# 更新日志

## v2.54（2026-09-05）

### 界面

- 播放 / 暂停键改为 **morphicons 风格矢量变形动画**：播放三角与暂停竖条平滑互转，
  不再生硬直接切换（含多房间接收端操作后的恢复）；
- 图标采用与 morphicons 一致的线条风格，通知栏图标与上一曲 / 下一曲图标不受影响。

## v2.53（2026-08-21）

### 界面

- 彻底移除应用内所有**水波纹动画**（按钮、列表项、设置栏目点击），
  并关闭页面上下滑动的越界水波纹效果；
- 小屏设备（如 480x640 的 PA03）自动启用紧凑布局：
  压缩标题、进度条与按钮尺寸，保证播放控制完整可见，
  盒子与正常平板的界面保持不变；
- 修复安卓 6.0 设备（如 PA03）状态栏未隐藏的问题。

## v2.52（2026-08-21）

### 多房间音频同步修复

- 修复接收端**音高被抬高 / 卡顿**：接收端播放改为直通（不做软件重采样），
  并加入设备速率自动校准，内容播放速率与主控一致；
- 修复主控与接收端**时间差随会话变化**：主控循环缓冲队列被限制在
  ~100ms（丢弃解码突发产生的多余超前音频），接收端队列同样限制，
  两边听到的延迟稳定一致；
- 操作后自动校准：播放 / 暂停 / 切歌 / 进度条 / 曲库切换后，
  立即刷新时钟偏移并对齐进度，不再等 3 秒兜底；
- 修复多房间下点击曲库中正在播放的歌曲：进度条与音频都会从 0 重新开始。

### 界面

- 接收端控制按钮（播放/暂停/上一曲/下一曲）等待发射端响应时显示加载转圈，
  播放键的转圈位于天蓝色圆形框内；
- 音量条左侧图标在音量为 0 时切换为静音图标；
- 应用强制横屏（允许 180° 翻转），所有页面以盒子 16:9 比例居中自适应，
  其它设备自动留边显示模糊背景；
- 隐藏状态栏（沉浸式），适配各厂商（含小米）的状态栏与手势条；

## v2.51（2026-08-20）

### 多房间音频修复

- 修复主控端**卡顿**：时间基准计算错误导致播放线程误差长期偏大，
  控制逻辑反复触发硬重同步（每 200ms 重启一次音频），现已修正；
- 修复主控端**失真**：主控播放自己的流时不再经过重采样，
  输出与源文件逐位一致；
- 修复接收端**失真/人声颤音**：接收端同样跳过软件重采样，
  由音频缓冲吸收微小硬件时钟漂移；
- 均衡器限幅器算法优化：峰值追踪改为 O(1) 滑动窗口，
  CPU 开销降低约一个数量级，输出声音不变；
- 多房间期间被静音的本地播放器直接跳过均衡器处理，进一步降低 CPU 占用；
- 新增多房间音频录音诊断钩子（adb 指令触发），便于排查音频问题。

## v2.50（2026-08-20）

### 界面

- 新增**深色背景模糊**：播放界面以专辑封面铺满屏幕并大幅虚化
  （未播放时使用专属占位封面），设置 → 界面 → 背景模糊 可切换
  “深色 / 关闭”，默认深色，效果应用到所有页面（含设置等）；
- 进入其他页面后，切歌时模糊背景会同步切换为新歌曲封面；
- 设置页设备名称输入框的描边颜色改为与按钮一致。

## v2.43（2026-08-20）

### 均衡器音质修复（二）

- 限幅器触发阈值调整到**满幅（0dBFS）**：此前阈值低于母带峰值，
  导致限幅器在普通播放（甚至 EQ 全平）时也频繁介入，产生“压泵感/失真”；
  现在仅在 EQ 真正把声音抬过 0dB 时才介入；
- 新增**自动补偿增益**：按曲线平均抬升量自动整体衰减，避免大增益曲线
  把声音顶满幅后限幅器持续压幅（实测最大压幅由 9.1dB 降至 4.4dB）；
- 限幅器前瞻加大到约 5.8ms、起音 0.3ms、释放 250ms，突发瞬态无过冲；
- 用设备真实录制输出（EQ 处理后、扬声器前的 PCM）离线验证，与
  Rubia 原曲对比，确认修复有效。

## v2.42（2026-08-20）

### 均衡器音质修复

- 均衡器峰值处理由“逐采样波形整形”改为**前瞻式限幅器**：
  约 1.45ms 前瞻、立体声联动、快起音（0.6ms）/ 慢释放（180ms），
  只压峰值不改变波形，消除高增益下的谐波失真；
- 重增益下失真由约 21.8% 降至 **0.00%**（算法仿真验证），
  本地播放与 AirPlay 路径同步生效；
- 修复单声道输入时可能越界写入的问题。

## v2.41（2026-08-20）

### 界面

- 关于页“开源项目”提示文案精简为“本项目使用了以下开源组件：”
  （四语同步更新）。

## v2.40（2026-08-19）

### 音乐传输

- 上传遇到**同名文件直接覆盖**，不再生成“(1)”副本；
- 未连接网络时传输页显示**“连接 WiFi”按钮**，一键跳转系统 WiFi 设置；
- 网页端上传前校验磁盘空间：若上传后剩余空间将**低于 80MB**，
  则拒绝上传并提示。

### 存储空间管理

- 设置 → 系统 新增**音乐库磁盘空间**显示（总空间 / 剩余空间）；
- 剩余空间**低于 80MB** 时，无线连接中的“音乐传输”按钮变灰，
  点击提示“存储空间小于80MB，请清理歌曲后再试”。

### 其他

- 应用图标分辨率翻倍（最大档 384px），高分辨率设备显示更清晰；
- 版本号更新为 2.40。

## v2.20（2026-08-19）

### 新增：音乐传输

- 设置 → 无线连接 → 音乐传输：右侧二维码、左侧 IP:端口；
- 手机 / 电脑扫码或浏览器输入地址即可上传音乐到本机曲库，仅接受音乐格式；
- 网页端支持**多文件选择、已选列表（可单独移除）、上传进度条、
  成功/失败提示、格式过滤**，样式与应用一致（天空蓝主题、左上角 BukaMusic）；
- 网页标签页显示软件 logo；上传完成后曲库自动刷新；
- **同名文件直接覆盖**，不再生成 “(1)” 副本。

### 新增：曲库管理

- 曲库长按歌曲进入多选模式，可**批量删除**音乐文件（含系统媒体库与
  USB 设备文件），删除后列表即时刷新；
- 删除的歌曲中包含当前播放曲时，自动切换到下一首未删除歌曲；
- 删除成功提示改为“已删除 n 首音乐”。

### 新增：四语界面与设置重构

- 设置新增 **界面** 栏目：**中 / 英 / 日 / 韩** 四语切换（全部界面文案、
  通知与后台服务状态同步翻译），播放界面应用列表图标开关移入此栏目；
- 设置新增 **关于** 栏目：展示开发者 GitHub、项目地址与开源组件清单；
- 设置栏目全部默认收起；“音乐传输”按钮移至“无线连接”菜单下。

### 界面优化

- 更换应用图标与网页标签图标；
- 传输页二维码白色边框改为圆角；
- 关于页应用图标改为圆角显示、信息仅展示不可点击；
- README 增加居中标题与高清版软件 logo。

### 曲库

- 打开曲库时**正在播放的歌曲自动置顶**并高亮：
  本地播放 / 多房间主控按文件精确定位，多房间接收端按标题/歌手匹配；
- 多房间接收端与 AirPlay 模式下隐藏底栏“曲库”按钮（曲库属于本机收藏，
  与正在接收的流无关）。

## v2.11（2026-08-18）

### 均衡器重写与音质修复

- **均衡器算法重写**：由“频带叠加 FIR”改为标准**双二阶峰值滤波器**，
  此前曲线增益被严重稀释（设定 +6.4dB 实际约 0dB），且叠加滤波器产生
  伪影；现在各频段增益真实生效、无异常频响凹陷；
- 输出增加**软限幅**：高增益下不再硬削波（峰值曾达 83524，硬削波 1.85%），
  消除类似“电流声”的失真；
- 本地 / AirPlay / 多房间三条播放路径各使用**独立均衡器实例**，切换互不干扰；
- 修复 AirPlay 路径未设置均衡器采样率导致滤波频段错位的问题；
- **多房间播放重新启用均衡器**（此前接收端/主控多房间输出未接入均衡器）。

### 本地切歌淡入淡出

- 本地切歌（含曲库选歌）加入 **1000ms 淡出 + 1000ms 淡入**（非交叉）；
- 快速连点合并到目标歌曲，不再逐首播放中间歌曲；淡入淡出完整执行不截断；
- 暂停/开始过渡期间切歌不再出现音量忽大忽小或歌曲反复跳回；
- 修复暂停状态下切歌静音的问题。

### 修复

- 修复曲库不自动刷新（新增文件不显示）的问题；
- 修复切歌后封面不刷新/旧封面残留的问题；
- 修复 AirPlay 进入/退出时约 3 秒电流声；
- 修复多房间停止时误触发本地淡入、与切歌淡入淡出争抢音量的问题。

## v2.00（2026-08-17）

### 新增：多房间同步播放

- 同一局域网内多台设备与主控**同步播放同一本地音频**：
  主控解码推流、每台接收端独立 NTP 时钟对时 + 重采样纠偏，误差毫秒级；
- 设备选择框保持勾选状态，取消勾选即断开对应设备；
- 接收端在多房间模式下可点击“断开连接”主动退出；
- 接收端可远程控制主控的播放 / 暂停 / 上一曲 / 下一曲 / 拖动进度条；
- 切歌 / 播放后 3 秒自动进行延迟校准。

### 新增：AirPlay 与多房间互抢/恢复

- 主控发射多房间时收到 AirPlay 信号 → 断开所有多房间设备；
  AirPlay 退出后自动重连之前设备并恢复播放（不在线的跳过）；
- 接收端多房间中收到 AirPlay 信号 → 多房间 2500ms 缓出 → AirPlay 2500ms 缓入；
  AirPlay 退出后 → AirPlay 2500ms 缓出 → 多房间 2500ms 缓入恢复；
- 多房间 / AirPlay 切换后元数据自动刷新还原。

### 界面优化

- 底栏左下“AirPlay”字样改为多房间按钮；
- 多房间设备选择增加“正在搜索设备”加载转圈；
- “开始同步”改为“确认”；
- 进入 AirPlay 模式自动隐藏多房间按钮；
- 所有滑条触摸区域加高，便于操作；
- 应用名称改为 **BukaMusic**；
- 设备名称默认使用系统设置内的设备名。

### 修复

- 修复多房间接收端播放位置恢复不显示进度条的问题；
- 修复源切换 / 断连时的双重声音与淡入淡出异常；
- 修复 AirPlay 会话被误关闭、元数据不刷新的问题。
